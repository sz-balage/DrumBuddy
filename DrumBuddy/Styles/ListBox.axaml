<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Design.PreviewWith>
        <Border Padding="20" Background="{StaticResource HeaderGray}">
            <!-- Add Controls for Previewer Here -->
            <ListBox>
                <ListBoxItem>Asd</ListBoxItem>
                <ListBoxItem>Asd</ListBoxItem>
                <ListBoxItem>Asd</ListBoxItem>
                <ListBoxItem>Asd</ListBoxItem>
                <ListBoxItem>Asd</ListBoxItem>
            </ListBox>
        </Border>
    </Design.PreviewWith>
    <Style Selector="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Margin" Value="0,0,0,10" />
    </Style>
    <Style Selector="ListBoxItem">
        <Setter Property="Margin" Value="1,0,1,8" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Background" Value="{DynamicResource CardItemColor}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DarkerGray}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>
    <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
        <Setter Property="Background">
            <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                <GradientStop Offset="0" Color="{DynamicResource Primary}" />
                <GradientStop Offset="1" Color="{DynamicResource Secondary}" />
            </LinearGradientBrush>
        </Setter>
    </Style>
    <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
        <Setter Property="Background">
            <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                <GradientStop Offset="0" Color="{DynamicResource Primary}" />
                <GradientStop Offset="1" Color="{DynamicResource Primary}" />
            </LinearGradientBrush>
        </Setter>
        <Setter Property="BorderBrush" Value="{DynamicResource Accent}" />
    </Style>
</Styles>